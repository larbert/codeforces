#include <iostream>
using namespace std;

const int MAX_N = 100005;

int main () {
    int t, n, a[MAX_N], b[MAX_N];
    cin >> t;
    while (t--) {
        cin >> n;
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
        cout << (4 | 2) << endl;
    }
    return 0;
}